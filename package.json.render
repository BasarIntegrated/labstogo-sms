{
  "name": "labstogo-sms-app",
  "version": "1.0.0",
  "description": "SMS Campaign Application - Frontend on Vercel, Backend on Render",
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
